<img
  src="{{ asset('images/logo-kalinga.svg') }}"
  alt=""
  class="kalinga-logo {{ $class ?? '' }}"
  @if(!empty($size)) width="{{ $size }}" height="{{ $size }}" @endif
  decoding="async"
  aria-hidden="true"
>
